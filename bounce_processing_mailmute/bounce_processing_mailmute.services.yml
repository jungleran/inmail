services:
  bounce.handler.mailmute:
    class: Drupal\bounce_processing_mailmute\MessageHandler\MailmuteMessageHandler
    arguments: ['@plugin.manager.sendstate', '@logger.channel.bounce_processing']
    tags:
      - { name: bounce.handler }
